# CI runtime dependencies — Van Gogh Living Scene
#
# This file is a portable subset of requirements.lock for GitHub Actions
# runners. It excludes hardware-dependent packages (picamera2, inky,
# ai-edge-litert, rembg) that cannot be installed on standard x86 runners.
#
# Hardware-dependent tests are marked with @pytest.mark.hardware and
# skipped in CI via `pytest -m "not hardware"`. Full compliance testing
# (including hardware) is performed on the target Pi via
# `scripts/compliance-check.sh` (see docs/release-checklist.md).
#
# Version floors match requirements.in. Hash pinning is applied to the
# full lockfile on the target, not to this CI subset.

Pillow>=12.0,<13.0
numpy>=2.2,<3.0
PyYAML>=6.0,<7.0

# Test framework
# Upper bounds widened 2026-04-05 to admit current majors verified on PyPI:
#   pytest 9.0.2, pytest-cov 7.1.0 (2026-03-21), pytest-mock 3.15.1
# All declare Python 3.13 support via classifiers.
pytest>=8.0,<10.0
pytest-cov>=5.0,<8.0
pytest-mock>=3.14,<4.0
